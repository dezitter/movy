.movy-search-result-item
  .movy-poster
    img(ng-src="{{ $ctrl.movie.small_poster_url }}")

  .movy-title
    | {{ $ctrl.movie.title }}

  .movy-release-date
    | ({{ $ctrl.movie.release_date }})

  button.pure-button(type="button" ng-click="$ctrl.onAdd()")= "+"
