.movy-search-result-item
  .movy-poster
    img(ng-src="{{ $ctrl.movie.small_poster_url }}")
    button.pure-button.movy-add(type="button" ng-click="$ctrl.onAdd()")= "+"

  .movy-title
    | {{ $ctrl.movie.title }}

  .movy-release-date
    | ({{ $ctrl.movie.release_date }})

